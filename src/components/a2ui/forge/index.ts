/**
 * The Forge — SkillForge design system component kit.
 * Import `./forge.css` once at the page root and wrap the page in
 * `<div className="forge-root">` to opt into the dark, namespaced system.
 */
export * from "./primitives";
export * from "./surface";
